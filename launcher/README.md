# Launcher

The screen a player sees: what is installed, what it looks like, and starting it.

It reads the card the way the console's own menu would - each game as a tile
with its own banner, taken from the disc the player supplied - and starts the
program built for that game.
